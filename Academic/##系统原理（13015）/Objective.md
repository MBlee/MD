## 概述

```shell
# 冯诺依曼结构基本思想
- 存储程序
- 五大部件(存储器/运算器/控制器/输入输出设备)
- 内部二进制(数据/指令-操作码-地址码/程序)
# 模型机基本硬件结构
- 主存/内存(指令/数据)
	通用寄存器组(GPRs/数据-运算结果)
	标志寄存器
	指令寄存器
	程序计数器
- 算术逻辑部件(ALU/信号)
- 控制元件(CU)
- 输入和输出设备
```

```shell
# 程序和指令执行过程
- 指令/存数指令/取数指令/加法指令/传送指令
- 操作码字段/地址码字段
```

```shell
# 程序开发与运行
```

```shell
# 层次结构/抽象层及其转换
# 核心层之间关联
# 不同用户
```

```shell
# 性能测试
- 吞吐率/响应时间/带宽/CPU时间/CPU性能
- 时钟周期
- 时钟频率
- CPI
# 用指令执行速度进行性能评估
# 用基准程序进行性能评估
```

## 数据表示与运算

```shell
数据机器级表示/运算(数值BCD/非数值)
>进位计数制/定浮点表示/编码规则
进位计数制>基数/位权(BOHD)>逢R进1/除基取余,上右下左
```

```shell
# 进制数转换
- R进制转10进制(位*基数次方相加)10
- 十进制转R进制(除基取余,上右下左)R
- 小数(乘积取整,上左下右)
- 2.8.16转换(3/4位)
```



## 程序转换及机器级表示

## 可执行文件的生成与加载执行

## 程序的存储访问

## 程序中IO操作实现
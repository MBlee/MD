



## Artery

```rust
fn main(){
  println!("{}",x);
  print!("{:?}",arr);
}
```

#### DataType

```rust
let mut x=0
const S:&str = "String"
i32/f64/bool/char/&str
let str=String::from("")
let str="".to_string()
format!("{}",str)
str.push_str("")
let str1=str.clone()
let str1=&mut str
```

```rust
let mut arr = ["",...]
let mut v = vec!["",...]
let tup = (...) => tup.0 => (,)
arr.len() => arr[]=x
v.push/pop/remove(i)
v.insert(i,"")
fn f()=>(String,i32){ ("",0) }
```

```rust
if{""}else{""}
match{1|2|3=>"",_=>""}
for i in 1..=9{}
loop{break "";}
while{continue;}
fn f(a:i32,b:f64)->(i32,f64){}
```




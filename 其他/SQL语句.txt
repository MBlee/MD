主键：非空且唯一
外键：与其他表联系

create data xxx
use xxx
create table xxx{
xx type,
primary key(xx),
foreign key(xx) references xxx(xx)
}
alter table xxx add constraint fk_sc foreign key(xx) reference cou(xx)
select * from xxx
